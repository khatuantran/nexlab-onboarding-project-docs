# BL-003 — Profile stats aggregation

<!-- template: 06-backlog-item-template.md@0.1 -->

## Metadata

- **Status**: Triaged
- **Priority**: P1
- **Requester**: @khatuantran11
- **Requested at**: 2026-05-16
- **Target milestone**: M3
- **Promoted to**: —

## Summary

As an **authenticated user**, I want **my `/profile` Stats card to show real counts** (projects I touched · features I documented · my total edits · sections completed by me), so that **the 4 hardcoded numbers `"4 / 18 / 42 / 12"` get replaced with truth**.

## Motivation

ProfilePage v4.1 (`31f8d3c`) ships a 4-tile `StatsCard` with hardcoded values. Every user sees the same fake numbers. Surfaced in CR-006 v4.6 mock audit (P1 — visible but lower priority than US-010 because users don't immediately rely on stat correctness).

## Proposed scope

**In scope**:

- New endpoint `GET /api/v1/me/stats` returning `{ projectsTouched: int, featuresDocumented: int, totalEdits: int, sectionsCompleted: int }`.
  - `projectsTouched` = DISTINCT projects where user has any `sections.updated_by = me`.
  - `featuresDocumented` = DISTINCT features where user authored ≥ 1 section.
  - `totalEdits` = COUNT(sections rows where updated_by = me). (Caveat: PUT overwrites updated_by → 1 edit per section per user, not historical revisions.)
  - `sectionsCompleted` = COUNT(sections where updated_by = me AND body != '').
- FE: ProfilePage Stats card reads from `useMeStats()` hook. Loading skeleton replaces hardcoded.

**Out of scope**:

- Historical edit count (no `audit_logs` table; PUT overwrites). Acceptable v1.
- Streak / trend graphs.
- Per-time-period filtering.
- Other-user stats (admin view).

## Acceptance hint

- **Given** user touched 3 projects, **When** GET /me/stats, **Then** `projectsTouched === 3`.
- **Given** brand-new user, **When** GET /me/stats, **Then** all 4 counts = 0.

## Dependencies

- **Blocked by**: nothing critical; US-011 contributors aggregation pattern provides repo template.
- **Blocks**: BL-004 (recent projects card uses same shaped projection).

## Promotion criteria

- [ ] User confirms 4 stat semantics match intent (esp. `totalEdits` = current rows not history).
- [ ] Aggregation query benchmarked < 50 ms for N = 100 user × 1000 sections.
- [ ] Cache strategy decided (none v1 / 60 s memo).

## Decision log

| Date       | Event    | Actor          | Notes                                               |
| ---------- | -------- | -------------- | --------------------------------------------------- |
| 2026-05-16 | Proposed | @khatuantran11 | FE mock audit (CR-006 v4.6). 4-tile hardcoded.      |
| 2026-05-16 | Triaged  | @khatuantran11 | P1. Defer behind US-010 + US-011 (foundation work). |
