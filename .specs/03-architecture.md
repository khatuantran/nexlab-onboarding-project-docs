# Architecture Summary

> **Status**: Placeholder. Nội dung sẽ được điền ở **Bước 3** dựa trên ADR-001.

---

## Nội dung sẽ có sau Bước 3

- Topology (monorepo layout, apps, packages).
- Data flow (client → API → DB, cache).
- Service boundaries (web / api / worker).
- External dependencies (Jira / Figma / GitHub embed).
- Environment & deployment (Docker Compose dev, K8s prod).

---

## ADR Index

- ADR-001 — Tech stack decision *(pending — Bước 3)*

*(Khi có ADR mới, thêm dòng mới ở đây và link tới file trong `adr/`.)*
