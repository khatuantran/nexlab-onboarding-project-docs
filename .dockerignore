# Trim build context for Fly deploys (CR-003 / ADR-002).
# Build context is the repo root so the Dockerfile can COPY pnpm
# workspace files. Anything not strictly needed at runtime is excluded.

# Node / pnpm
node_modules
**/node_modules
.pnpm-store

# Build output (we run via tsx, no compiled dist)
dist
**/dist

# Local env files (secrets injected via Fly secrets at runtime)
.env
.env.local
.env.test
.env.*.local
**/.env
**/.env.local
**/.env.test
**/.env.*.local

# Tests + e2e (not needed in prod image)
**/tests
**/__tests__
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
e2e
playwright.config.ts
playwright-report
test-results

# Tooling
.git
.github
.husky
.vscode
.specs
docs
templates
scripts
**/coverage
**/*.log
*.md
README.md
SECURITY.md
CONTRIBUTING.md

# Frontend (separate Netlify deploy)
apps/web
netlify.toml

# Local data
data
infra/docker/data
