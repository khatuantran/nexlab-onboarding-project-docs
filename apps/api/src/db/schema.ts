/**
 * Drizzle schema — single source of truth for DB structure.
 *
 * T3: empty scaffold so drizzle-kit can run end-to-end.
 * T5 lands the actual tables: users, projects, features, sections.
 *
 * Never hand-edit migration files in ./migrations/ — always
 * `pnpm db:generate` after changing this file.
 */

export {};
